*.iml
.gradle
/local.properties
.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/testdata
.codex/
.codex-*
.bootstrap-env
.claude/
# Local secrets for the live-server smoke tests (HOSHI_KV_TOKEN etc).
# Copy `.hoshi-sync-secret.env.example` to `.hoshi-sync-secret.env` and fill it in.
.hoshi-sync-secret.env

# Fork release signing keystore + procedure. Local-only — the upstream copy
# lives in Dropbox at hoshi-manga-bundle/android-debug.keystore. See
# signing/README.md for the exact two-step build-then-re-sign flow that
# keeps the cert matching every previously-released fork APK.
/signing/
tools/__pycache__/
app/src/androidTest/assets/
